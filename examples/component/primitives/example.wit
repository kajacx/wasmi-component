package wasmi-component:examples@0.1.0;

interface funcs {
    add-s32: func(a: s32, b: s32) -> s32;
}

world example {
    export funcs;

    export add-u32: func(a: u32, b: u32) -> u32;

    export additional: interface {
        add-f32: func(a: f32, b: f32) -> f32;
    }
}
